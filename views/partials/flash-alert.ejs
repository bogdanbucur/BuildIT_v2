<script>
    window.flashAlerts = window.flashAlerts || [];
    <% locals.flash.messages.forEach(function (msg) { %>
    <% if (msg.type && msg.message) { %>
    window.flashAlerts.push(<%-JSON.stringify(msg)%>);
    <% } %>
    <% }) %>
</script>